#pragma once
// Stub for the JUCE umbrella header.
//
// Source/PluginParameters.h and three of the DSP sources open with
// `#include <JuceHeader.h>`. With `-Ilv2/src` on the command line that
// resolves to this file instead of a real JUCE checkout, so the DSP compiles
// for LV2 without a single edit to Source/. See JuceCompat.h for what is
// actually provided and why that is enough.
#include "JuceCompat.h"
